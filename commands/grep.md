# Grep

```powershell
findstr "$search_str" $file
```

**grep**
* `Where-Object { condition }`
    * `$_`: current line
    * `-not`
    * `-like`
    * `-match`
    * `-eq`
    * `-ne`
    * `-gt`
    * `-ge`
    * `-lt`
    * `-le`
    * `-and`
    * `-or`
    * `-nomatch 'REGEX'`