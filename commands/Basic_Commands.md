
**mv**
ps: `move`, `Move-Item`

**rm**

ps: `Remove-Item`
    * `-Recurse`
    * `-Force`

ps: `$HOME`
> No ~

### Power
ps: `Restart-Computer`
cmd: `shutdown /r /t 0`

### Variable Expansion
```powershell
$name = "Alice"
$message = "Hello, $name!"
Write-Output $message
```

* Literal `'literal'`

### Regex
* ps: `*`

### Disks

`C`: 

### Path
`\` style.